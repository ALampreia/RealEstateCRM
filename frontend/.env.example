# Example environment variables — do NOT put real secrets here.
# Copy this file to .env locally and fill the real values (do NOT commit .env)

# Backend API endpoint
API_URL=https://api.example.com

# Public-only keys (non-secret)
PUBLIC_API_KEY=your_public_key_here

# Example feature flags
FEATURE_FLAG_X=true

# Do NOT put private keys, database passwords, or long-lived tokens here.
